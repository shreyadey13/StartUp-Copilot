# AI Startup Copilot Frontend

Production-grade Next.js app with TypeScript, TailwindCSS, shadcn-style UI primitives, React Query, Zustand, authentication, responsive layout, and dark mode.

## Run

```bash
cd frontend
npm install
cp .env.example .env.local
npm run dev
```

Open `http://localhost:3000`.

